#include <iostream>
using namespace std;

int main() {
    // pilha lista e fila ; (as 3 são iguais, a unica diferença é a disposição dos elementos)
    // LISTA :
    // lista são conjuntos de elementos, objetos, variaveis, tarefas ou qualquer coisa que possa enumerar e formar um conjunto. É igual uma lista de compras
    
    // listas encadeadas
    // duplamente encadeadas - circulares
    
    // comportamento de uma lista
    // fim de uma lista é o começo de outra
    
    // Filas
    
    //pilhas
    // topo = null;
    //pilhas podem ser implementadas em listas encadeadas ou vetores

    return 0;
}
